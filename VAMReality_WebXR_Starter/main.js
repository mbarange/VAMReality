document.addEventListener("DOMContentLoaded", () => {
    // All your JS logic goes here, after the DOM is ready
    initializeScenarioManager();
    renderFlowGraph();
  });
// Optional: main.js for future global logic
console.log("📦 VAMReality main.js loaded.");
